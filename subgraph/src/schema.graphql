type Image {
  id: ID!
  imageUrl: String!
}

type Video {
  id: ID!
  videoUrl: String!
}

union Asset = Image | Video

type Query {
  hello: String
}

type Mutation {
  createImagesAndVideos: [Asset]
}
